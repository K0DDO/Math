# Вопрос 5. Линейные, билинейные и квадратичные формы. Диагонализация

## Что спрашивают на экзамене

- Определения линейной, билинейной и квадратичной формы.
- Матрица квадратичной формы (как выписать из многочлена).
- Запись $Q=X^TAX$.
- Приведение к каноническому (диагональному) виду **через собственные значения** (ортогональное преобразование).
- Иногда — связь с самосопряжённым оператором.

Источники:

- [Квадратичные формы](https://mathprofi.ru/kvadratichnye_formy.html)
- [Метод ортогонального преобразования КФ](https://mathprofi.ru/metod_ortogonalnogo_preobrazovaniya_kf.html)

---

## Все понятия с нуля

**Форма** здесь — функция от координат вектора (или двух векторов), заданная многочленом определённого вида.

**Линейная форма** на $V$ — функция $L:V\to\mathbb{R}$, линейная:

$$L(\alpha\mathbf{x}+\beta\mathbf{y})=\alpha L(\mathbf{x})+\beta L(\mathbf{y}).$$

В координатах: $L(x)=\alpha_1 x_1+\ldots+\alpha_n x_n=A X$, где $A$ — строка коэффициентов. ([урок](https://mathprofi.ru/kvadratichnye_formy.html))

**Билинейная форма** — функция $B(\mathbf{x},\mathbf{y})$ от двух векторов, линейная по каждому аргументу отдельно. В координатах

$$B(\mathbf{x},\mathbf{y})=X^{\top} B Y=\sum_{i,j}b_{ij}x_i y_j.$$

Если $B^{\top}=B$, форма **симметричная**. ([урок](https://mathprofi.ru/kvadratichnye_formy.html))

**Квадратичная форма** — однородный многочлен второй степени от координат:

$$Q(x_1,\ldots,x_n)=\sum_{i=1}^{n}a_{ii}x_i^2+2\sum_{i<j}a_{ij}x_i x_j.$$

Её можно получить из симметричной билинейной формы: $Q(\mathbf{x})=B(\mathbf{x},\mathbf{x})$. ([урок](https://mathprofi.ru/kvadratichnye_formy.html))

**Матрица квадратичной формы** — симметрическая матрица $A=(a_{ij})$, для которой

$$Q=X^TAX.$$

Источник: [квадратичные формы](https://mathprofi.ru/kvadratichnye_formy.html).

**Канонический вид** квадратичной формы — вид без произведений разных переменных:

$$Q=\lambda_1 y_1^2+\lambda_2 y_2^2+\ldots+\lambda_n y_n^2.$$

Источник: [метод ортогонального преобразования КФ](https://mathprofi.ru/metod_ortogonalnogo_preobrazovaniya_kf.html).

**Ортогональное преобразование** — замена $X=UY$, где $U^TU=E$ (поворот / отражение осей; сохраняет длины и углы). ([урок](https://mathprofi.ru/metod_ortogonalnogo_preobrazovaniya_kf.html))

---

## Теория подробно

### Как составить матрицу квадратичной формы

Правило:

- коэффициент при $x_i^2$ идёт на диагональ: $a_{ii}$;
- коэффициент при произведении $x_i x_j$ ($i\neq j$) делится пополам: $a_{ij}=a_{ji}=\frac12$ этого коэффициента.

Пример: $Q=2x_1^2-4x_1 x_2+5x_2^2$.

$$A=\begin{pmatrix} 2 & -2 \\\\ -2 & 5 \end{pmatrix},$$

потому что $-4x_1x_2$ даёт два симметричных места по $-2$.

Проверка: 

$$X^TAX=\begin{pmatrix}x_1 & x_2\end{pmatrix}\begin{pmatrix}2 & -2 \\\\ -2 & 5\end{pmatrix}\begin{pmatrix}x_1 \\\\ x_2\end{pmatrix}=2x_1^2-4x_1x_2+5x_2^2$$

.

Ранг формы — ранг матрицы $A$. Определитель $\det A$ иногда называют дискриминантом формы (для $n=2$).

### Связь с линейным оператором

Симметричная матрица $A$ задаёт самосопряжённый оператор (в ортонормированном базисе). Квадратичная форма — «скалярное произведение» $\,(A\mathbf{x},\mathbf{x})$. Поэтому теория собственных значений симметричных матриц напрямую применяется к $Q$.

### Диагонализация через собственные значения

Так как $A=A^{\top}$, существует ортонормированный собственный базис: ортогональная матрица $U$ ($U^{-1}=U^{\top}$) из собственных векторов-столбцов такая, что

$$U^{\top} A U=\Lambda=\mathrm{diag}(\lambda_1,\ldots,\lambda_n).$$

Замена переменных $X=UY$ (новые координаты $Y$ — координаты в собственном базисе) даёт

$$Q=X^TAX=Y^{\top}(U^{\top} A U)Y=Y^{\top}\Lambda Y=\lambda_1 y_1^2+\ldots+\lambda_n y_n^2.$$

Это **канонический вид методом ортогонального преобразования**. На mathprofi его выделяют как наиболее «геометричный»: оси поворачивают вдоль главных направлений формы. ([урок](https://mathprofi.ru/metod_ortogonalnogo_preobrazovaniya_kf.html))

Знаки $\lambda_i$ определяют знакоопределённость (положительно определённая — все $\lambda_i>0$ и т.д.; подробнее — критерий Сильвестра в других билетах).

Другие методы приведения (Лагранж, Якоби) тоже приводят к сумме квадратов, но замена не обязательно ортогональна; на этом билете акцент — на собственных числах.

---

## Как решать / алгоритм

**А. Выписать матрицу формы из многочлена**

1. Найти все $x_i^2$ и $x_i x_j$.
2. Заполнить симметричную $A$ по правилу выше.
3. Проверить раскрытием $X^TAX$.

**Б. Привести к канону через собственные значения**

1. Составить симметричную матрицу $A$.
2. Найти собственные значения: $\det(A-\lambda E)=0$.
3. Для каждого $\lambda$ найти собственные векторы; внутри кратных подпространств — ортогонализовать; все векторы нормировать.
4. Составить ортогональную $U$ из ортов-столбцов (порядок столбцов = порядок $\lambda$ на диагонали).
5. Записать $Q=\lambda_1 y_1^2+\ldots+\lambda_n y_n^2$ и замену $X=UY$.
6. Проверка: $U^TAU=\Lambda$ или подстановка замены в $Q$.

На маленьких задачах ($2\times 2$) нормировку иногда опускают в черновике, но для ортогональности $U^TU=E$ нормировка нужна; можно указать ортогональную замену с нормированными векторами.

---

## Разобранный пример

**Задача.** Привести к каноническому виду ортогональным преобразованием форму

$$Q=5x^2+4xy+2y^2.$$

**Шаг 1. Матрица.** Коэффициент при $xy$ равен $4$, значит $a_{12}=a_{21}=2$:

$$A=\begin{pmatrix} 5 & 2 \\\\ 2 & 2 \end{pmatrix}.$$

**Шаг 2. Собственные значения.**

$$\det\begin{pmatrix} 5-\lambda & 2 \\\\ 2 & 2-\lambda \end{pmatrix}=(5-\lambda)(2-\lambda)-4=\lambda^2-7\lambda+6=0.$$

$$\,(\lambda-1)(\lambda-6)=0\implies\lambda_1=1,\ \lambda_2=6.$$

**Шаг 3. Собственные векторы.**

Для $\lambda=1$: 

$$\,(A-E)X=0\Rightarrow\begin{pmatrix}4 & 2 \\\\ 2 & 1\end{pmatrix}X=0\Rightarrow 2x+y=0$$

.  
Вектор $\,(1,-2)$; длина $\sqrt{5}$. Орт: 

$$u_1=\frac{1}{\sqrt{5}}\begin{pmatrix}1 \\\\ -2\end{pmatrix}$$

.

Для $\lambda=6$: 

$$\,(A-6E)X=0\Rightarrow\begin{pmatrix}-1 & 2 \\\\ 2 & -4\end{pmatrix}X=0\Rightarrow -x+2y=0$$

.  
Вектор $\,(2,1)$; длина $\sqrt{5}$. Орт: 

$$u_2=\frac{1}{\sqrt{5}}\begin{pmatrix}2 \\\\ 1\end{pmatrix}$$

.

Проверка ортогональности: $1\cdot 2+(-2)\cdot 1=0$.

**Шаг 4. Замена и канон.**

$$U=\frac{1}{\sqrt{5}}\begin{pmatrix} 1 & 2 \\\\ -2 & 1 \end{pmatrix},\qquad \begin{pmatrix} x \\\\ y \end{pmatrix}=U\begin{pmatrix} y_1 \\\\ y_2 \end{pmatrix}.$$

Канонический вид:

$$Q=1\cdot y_1^2+6\cdot y_2^2=y_1^2+6y_2^2.$$

**Проверка следа/определителя:** $\mathrm{tr}\,A=7=1+6$, $\det A=6=1\cdot 6$.

---

## Практика

**1.** Выпишите матрицу формы $Q=x_1^2-6x_1x_2+x_2^2$.  
*Ответ:* 

$$A=\begin{pmatrix}1 & -3 \\\\ -3 & 1\end{pmatrix}$$

.

**2.** Приведите $Q=x^2+y^2$ к канону. Что получится?  
*Ответ:* уже канон; $A=E$, $\lambda=1,1$. Любой ортонормированный базис годится.

**3.** $Q=xy$. Выпишите $A$ и найдите канонический вид через собственные значения.  
*Ответ:* 

$$A=\begin{pmatrix}0 & 1/2 \\\\ 1/2 & 0\end{pmatrix}$$

, $\lambda=\pm\frac12$, канон $\frac12 u^2-\frac12 v^2$ (после ортогональной замены).

**4.** Верно ли, что любая квадратичная форма приводится к канону ортогональным преобразованием над $\mathbb{R}$?  
*Ответ:* да, потому что матрица симметрична и всегда диагонализуема ортогональной матрицей.

---

## Что сказать устно за 1 минуту

Линейная форма — линейная функция от вектора; билинейная — линейна по каждому из двух аргументов; квадратичная — однородный многочлен второй степени $Q=X^TAX$ с симметричной матрицей $A$. Матрицу собирают так: квадраты — на диагональ, коэффициент при $x_ix_j$ делят пополам на симметричные места. Так как $A$ симметрична, её можно диагонализовать ортогональной матрицей из собственных векторов: замена $X=UY$ приводит $Q$ к сумме $\lambda_i y_i^2$ — каноническому виду. Это метод ортогонального преобразования квадратичной формы.

---

## Источник

- [Квадратичные формы — mathprofi](https://mathprofi.ru/kvadratichnye_formy.html)
- [Метод ортогонального преобразования КФ](https://mathprofi.ru/metod_ortogonalnogo_preobrazovaniya_kf.html)
- [Собственные значения и векторы](https://mathprofi.ru/sobstvennye_znachenija_i_sobstvennye_vektory.html)
- Глоссарий: `../00_bazovye_ponyatiya.md`
